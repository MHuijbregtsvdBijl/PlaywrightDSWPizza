import { test,expect, Page } from '@playwright/test';
//codeVoorbeeld: PW is identiek aan Cypress, muv van eerder genoemde page.goto/cy.visit en assert-statement

test('Gebruik waardes', async ({ page }) => {
  //Type hier je code
});